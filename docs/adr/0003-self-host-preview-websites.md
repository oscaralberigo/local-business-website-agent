# Self-host published preview websites

Published previews will be served from the operator's own configured server and purchased domain rather than a managed preview platform. The implementation should keep publishing behind a deployment adapter so the server can be configured later without changing the agent workflow, while still preserving unguessable preview URLs, noindex behavior, publish metadata, and unpublish support.
